<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="view_leave_allocation_wizard_form" model="ir.ui.view">
            <field name="name">hr.leave.allocation.wizard.form</field>
            <field name="model">hr.leave.allocation.wizard</field>
            <field name="arch" type="xml">
                <form string="Leave Allocation Wizard">
                    <sheet>
                        <group>
                            <field name="allocation_ids" widget="many2many_tags"/>
                        </group>

                        <group>
                            <group>
                                <field name="start_date" required="1"/>
                                <field name="end_date" required="1"/>
                            </group>
                            <group>
                                <field name="duration"/>
                            </group>
                        </group>
                    </sheet>

                    <footer>
                        <button name="updateAllSelectedAllocations" type="object" string="Apply" class="btn-primary" confirm="Are you sure to update"/>

                        <button string="Cancel" special="cancel" class="btn-secondary"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="ir_action_leave_allocation_update" model="ir.actions.server">
            <field name="name">Leave Allocation Update</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="hr_holidays.model_hr_leave_allocation"/>
            <field name="binding_model_id" ref="hr_holidays.model_hr_leave_allocation"/>
            <field name="state">code</field>

            <field name="code">action=model.update_validity_action()</field>
        </record>

    </data>
</odoo>