<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="hr_recruitment_sign_view_inherited" model="ir.ui.view">
        <field name="name">Add to_be_reversed and reversal_id fields</field>
        <field name="model">hr.recruitment.sign.document.wizard</field>
        <field name="inherit_id" ref="hr_recruitment_sign.hr_recruitment_sign_view"/>
        <field name="arch" type="xml">
            <field name="applicant_id" position="replace">
                <field
                    name="applicant_id"
                    attrs="{'invisible': [('applicant_ids', '!=', False)]}"
                    readonly="1"
                />
                <field
                    name="applicant_id"
                    attrs="{'invisible': [('applicant_ids', '!=', False)]}"
                    readonly="1"
                />
            </field>
        </field>
    </record> 

</odoo>